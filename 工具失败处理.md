# 工具失败处理

Hades OpenSpec Workflow 的原则是：工具失败时降级或暂停，不绕过 PRD、OpenSpec、确认和 review gate。

## OpenSpec 不存在

现象：

```text
openspec: command not found
```

处理：

```bash
npm install -g @fission-ai/openspec@latest
```

如果目标项目未初始化：

```bash
cd <your-project>
openspec init
openspec config profile
openspec update
```

不要因为 OpenSpec 暂时不可用就直接实现行为变更。可以先生成 PRD，等 OpenSpec 可用后再 propose。

## OpenSpec status 或 instructions 失败

处理：

1. 记录失败命令和错误摘要。
2. 检查 change name 是否正确。
3. 运行 `openspec list` 查看 active changes。
4. 如果 artifacts 缺失，提示用户使用 `/opsx:continue` 或继续生成 artifacts。
5. 如果 schema 不匹配，不要假设文件名，优先依据 CLI 输出。

## 外部 connector 不可用

适用于 Asana、Notion、Figma、Stripe、Cloudflare、GitHub、Canva 或内部数据库 MCP。

处理：

1. 明确说明缺少哪个 connector 或 MCP。
2. 说明当前无法自动读取哪些信息。
3. 请求用户粘贴必要内容，或继续基于已有上下文生成 PRD 草稿。
4. 不编造外部平台数据。

示例：

```text
当前会话没有可用的 Asana connector，我不能直接读取任务内容。请粘贴任务描述、验收标准和相关评论；我会先基于这些信息生成 PRD 草稿。
```

内部数据库 MCP 不可用时：

```text
当前会话没有可用的内部数据库 MCP，我不能直接查询业务数据。请提供脱敏后的表结构、样本数据或查询结果；我会先基于这些信息更新 PRD / OpenSpec。
```

## PRD 与 OpenSpec 不一致

处理：

1. 暂停实现。
2. 使用 `sync-guard` 判断影响面。
3. 更新 PRD 对应拆分文档。
4. 更新 OpenSpec artifacts。
5. 让用户确认更新后的事实来源。
6. 再继续实现。

不要只在聊天里说“以后按新规则”，必须同步到文件。

## CodeGraph 不可用

适用于仓库需要调用链、符号定位、影响面分析，但没有 `.codegraph/` 或 `codegraph explore` 不可用。

处理：

1. 说明当前无法使用 CodeGraph 生成调用链证据。
2. 使用 `rg`、文件阅读和项目测试做降级分析。
3. 如果变更影响面大、跨模块或调用关系不清，暂停并让用户决定是否先初始化 CodeGraph。
4. 不要声称已经完成 CodeGraph 级别的影响面分析。

## Java / Spring / MyBatis 构建或测试失败

处理：

1. 先判断失败是否由本次改动引入。
2. Maven、Gradle、Spring Boot、annotation processor 或测试编译失败时，使用 `java-build-fix` 做最小修复。
3. 如果是本次改动导致，修复并重跑验证。
4. 如果是既有失败，记录证据和影响范围。
5. 如果失败改变实现范围，暂停并更新 PRD / OpenSpec。

不能把失败测试说成通过。

## SQL / MySQL 证据不足

适用于 MyBatis SQL、列表、搜索、统计、导出、分页、排序、索引、EXPLAIN、数据库写入或 DDL。

处理：

1. 使用 `sql-performance-review` 判断是否缺少索引、稳定排序、分页策略、N+1 或大结果集证据。
2. 涉及真实 MySQL MCP、写入、DELETE、DDL、EXPLAIN 时，使用 `mysql-db-guard`。
3. MCP 不可用时，请用户提供脱敏 schema、样本 SQL、EXPLAIN 或慢查询证据。
4. 证据不足时不得通过 `pr-quality-gate`。

## 实现后自检不通过

适用于 `post-implementation-check` 输出 `NEEDS_SYNC`、`NEEDS_FIX` 或 `NEEDS_EVIDENCE`。

处理：

1. `NEEDS_SYNC`：调用 `sync-guard`，先同步 PRD / OpenSpec，再继续。
2. `NEEDS_FIX`：先修复代码、配置、任务勾选或删除多做内容，再重新自检。
3. `NEEDS_EVIDENCE`：补测试、`EXPLAIN`、手动验证记录、影响行数或回滚方案。
4. 自检不通过时，不进入最终验证、Review Gate、PR Gate 或归档。

## PR Quality Gate 不通过

处理：

1. 暂停归档或提交 PR。
2. 列出未满足项：实现后自检、OpenSpec tasks、PRD/OpenSpec 同步、构建、测试、Java review、security review、SQL evidence、MySQL guard、回滚方案。
3. 能本地修复的按最小范围修复并重跑验证。
4. 涉及需求或设计变化时，先同步 PRD / OpenSpec，再继续实现。
5. 只有质量门禁通过后，才提示用户可以进入最终 review 或归档。

## GitHub push 失败

处理：

1. 检查是否有远程：

```bash
git remote -v
```

2. 如果没有远程，让维护者先创建 GitHub 仓库，再添加：

```bash
git remote add origin https://github.com/<owner>/hades-openspec-workflow.git
git push -u origin main
```

3. 如果认证失败，改用 SSH 或登录 GitHub CLI / Git credential。

## 高风险写操作

以下操作必须先暂停并确认：

- Stripe 退款、订阅变更、支付状态修改
- Cloudflare DNS、生产部署、Workers 路由变更
- 数据库 DELETE、DDL、批量 UPDATE
- Notion / Asana / GitHub 的状态写入
- 任何生产配置变更

确认内容至少包括：

```text
目标：
影响范围：
具体操作：
回滚方式：
验证方式：
```
