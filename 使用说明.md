# 使用说明

Hades OpenSpec Workflow 的核心原则是：先 PRD，后 OpenSpec；先确认，后实现；实现完成后进入 review gate；归档必须由用户触发。

## 一句话启动

```text
请使用 Hades OpenSpec Workflow 处理这个需求。先分析需求并生成 PRD 文档，不要直接写代码。
```

如果需求来自外部平台，可以直接给链接：

```text
请使用 Hades OpenSpec Workflow 处理这个 Asana 任务：https://app.asana.com/...
```

```text
请根据这个 Figma 设计稿生成 PRD 和 OpenSpec，不要先实现：https://www.figma.com/...
```

```text
请根据这个 GitHub PR 的背景做 review gate，不要创建 issue：https://github.com/<org>/<repo>/pull/123
```

## 入口触发规则

| 输入特征 | 触发倾向 |
| --- | --- |
| `asana.com` 链接或 Asana 任务描述 | 读取 Asana 任务，作为 PRD 来源。 |
| Notion 链接或产品文档 | 读取 Notion，提取背景和约束。 |
| Figma 链接或 UI/交互需求 | 读取 Figma，沉淀原型行为规则。 |
| Stripe 支付、订阅、退款语义 | 优先读取 Stripe 上下文。 |
| Cloudflare 部署、DNS、Workers、Pages 语义 | 优先检查 Cloudflare 状态。 |
| GitHub PR 链接 | 读取 PR、diff、review 上下文；不创建 Issue。 |
| 内部 ID、订单号、用户号、报表号 | 在已配置 MCP 的情况下使用 `db-query-guard` 查询内部数据库。 |
| 纯用户对话 | 直接进入需求分析和 PRD 草稿。 |

触发不只依赖链接，也可以由用户明确指定：

```text
这个需求不用读 Asana，以我下面说的为准。
```

```text
先读 Notion，再结合 Figma 生成 PRD。
```

```text
只看 GitHub PR，不要访问 Stripe。
```

如果链接和用户指令冲突，以用户最新明确指令为准，并把决策写入 PRD 或 OpenSpec。

## 内部数据库 MCP

当需求需要查内部数据时，可以说：

```text
请通过内部数据库 MCP 查询订单 123 的状态，只读查询，不要执行写操作。
```

或：

```text
请用数据库 MCP 验证这个报表统计口径，查询结果只需要总结，不要输出敏感明细。
```

默认允许：

```sql
SELECT
SHOW
DESCRIBE
EXPLAIN
```

`INSERT`、`UPDATE`、`DELETE`、DDL 都必须先展示 SQL、影响范围、回滚方式和验证方式，并等待用户明确确认。

如果数据库查询结果改变需求范围、验收标准、接口字段、数据库设计或实现约束，必须先同步 PRD 和 OpenSpec，再继续实现。

## PRD 阶段

PRD 文件必须在目标项目根目录：

```text
docs/<中文功能名>/
```

固定创建：

```text
<功能名>文档入口.md
<功能名>需求PRD.md
```

按影响范围创建：

```text
<功能名>原型行为规则.md
<功能名>前端接口设计.md
<功能名>数据库设计.md
<功能名>技术设计.md
```

没有涉及的范围不要创建空文档，改在 `<功能名>文档入口.md` 的 `未涉及范围` 中说明原因。

PRD 写完后必须停下来让用户确认：

```text
PRD 文档已生成，请确认是否可以进入 OpenSpec。
```

## OpenSpec 阶段

PRD 确认后，用户可以说：

```text
PRD 已确认，请生成 OpenSpec。
```

或：

```text
/opsx:propose add-order-export
```

`/opsx:propose` 支持两种输入：

```text
/opsx:propose <kebab-case-change-name>
/opsx:propose <natural-language-description>
```

如果用户说的是自然语言，Codex 需要转换成 kebab-case change name。

执行规则：

```bash
openspec new change "<name>"
openspec status --change "<name>" --json
openspec instructions <artifact-id> --change "<name>" --json
openspec status --change "<name>"
```

必须按 OpenSpec status 返回的依赖顺序生成 artifacts，并在所有 `applyRequires` 完成后停下来让用户确认。

## 实现阶段

OpenSpec 确认后，用户可以说：

```text
执行计划。
```

或：

```text
/opsx:apply
```

或：

```text
/opsx:apply add-order-export
```

实现前必须：

1. 解析 change name。
2. 运行 `openspec status --change "<name>" --json`。
3. 运行 `openspec instructions apply --change "<name>" --json`。
4. 读取 `contextFiles` 中列出的所有文件。
5. 展示进度和剩余任务。
6. 按 task 循环实现。
7. 每完成一个 task，立即把 checkbox 从 `- [ ]` 改成 `- [x]`。

如果任务不清楚、PRD/OpenSpec 不一致、contextFiles 缺失、测试失败改变范围，必须暂停并说明原因。

## Review 阶段

实现和验证完成后必须告诉用户：

```text
改动已完成并通过验证，现在可以进行 review。你可以让我执行代码审查、根据反馈修改，或准备归档计划。
```

用户可以继续说：

```text
执行代码审查。
```

```text
根据 review 反馈修改。
```

```text
归档计划。
```

## 归档阶段

用户明确要求归档时才执行：

```text
归档计划。
```

或：

```text
/opsx:archive
```

归档前必须验证：

```bash
openspec validate <name>
openspec archive <name> --yes
```

如果项目维护 PRD 入口状态，也要同步更新 `<功能名>文档入口.md` 中的 OpenSpec 信息。

## 常用提示词

完整需求：

```text
请使用 Hades OpenSpec Workflow 处理这个需求：<需求内容>。先生成 PRD 文档，不要写代码。
```

PRD 改动：

```text
刚刚的规则改了：<新规则>。请同步更新 PRD 和 OpenSpec，然后再继续。
```

生成 OpenSpec：

```text
PRD 已确认，请创建 OpenSpec change，change 名称按 OpenSpec 自己规则生成。
```

执行实现：

```text
OpenSpec 已确认，请执行计划。
```

审查：

```text
请进入 review gate，先做代码审查，重点看 bug、回归风险和缺失测试。
```

归档：

```text
review 已确认，可以归档计划。
```
