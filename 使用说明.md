# 使用说明

Hades Openspec Java Workflow 的核心原则是：先 PRD，后 OpenSpec；先确认，后实现；代码写完后先做实现后自检，再验证并进入 review gate；归档必须由用户触发。

本插件已内置 Java/Spring Boot/MyBatis/MySQL 交付规范：AI 写代码纪律、Java 编码规范、Spring Boot 服务分层、安全评审、MySQL MCP 安全、SQL 性能评审、测试策略、Java 后端 Review、PR Quality Gate 和大重构流程。

## 一句话启动

```text
请使用 Hades Openspec Java Workflow 处理这个需求。先分析需求并生成 PRD 文档，不要直接写代码。
```

如果需求来自外部平台，可以直接给链接：

```text
请使用 Hades Openspec Java Workflow 处理这个 Asana 任务：https://app.asana.com/...
```

```text
请根据这个 Figma 设计稿生成 PRD 和 OpenSpec，不要先实现：https://www.figma.com/...
```

```text
请根据这个 GitHub PR 的背景做 review gate，不要创建 issue：https://github.com/<org>/<repo>/pull/123
```

## 入口触发规则

| 输入特征 | 触发倾向 |
| --- | --- |
| `asana.com` 链接或 Asana 任务描述 | 读取 Asana 任务，作为 PRD 来源。 |
| Notion 链接或产品文档 | 读取 Notion，提取背景和约束。 |
| Figma 链接或 UI/交互需求 | 读取 Figma，沉淀原型行为规则。 |
| Stripe 支付、订阅、退款语义 | 优先读取 Stripe 上下文。 |
| Cloudflare 部署、DNS、Workers、Pages 语义 | 优先检查 Cloudflare 状态。 |
| GitHub PR 链接 | 读取 PR、diff、review 上下文；不创建 Issue。 |
| 内部 ID、订单号、用户号、报表号 | 在已配置 MCP 的情况下使用 `mysql-db-guard` / `db-query-guard` 查询内部数据库。 |
| 纯用户对话 | 直接进入需求分析和 PRD 草稿。 |

触发不只依赖链接，也可以由用户明确指定：

```text
这个需求不用读 Asana，以我下面说的为准。
```

```text
先读 Notion，再结合 Figma 生成 PRD。
```

```text
只看 GitHub PR，不要访问 Stripe。
```

如果链接和用户指令冲突，以用户最新明确指令为准，并把决策写入 PRD 或 OpenSpec。

## 内部数据库 MCP

当需求需要查内部数据时，可以说：

```text
请通过内部数据库 MCP 查询订单 123 的状态，只读查询，不要执行写操作。
```

或：

```text
请用数据库 MCP 验证这个报表统计口径，查询结果只需要总结，不要输出敏感明细。
```

MySQL 项目优先使用 `mysql-db-guard`。默认允许：

```sql
SELECT
SHOW
DESCRIBE
EXPLAIN
```

`INSERT`、`UPDATE`、`DELETE`、DDL 都必须先展示 SQL、影响范围、回滚方式和验证方式，并等待用户明确确认。

如果数据库查询结果改变需求范围、验收标准、接口字段、数据库设计或实现约束，必须先同步 PRD 和 OpenSpec，再继续实现。

## Java / Spring Boot / MyBatis 规则

实现 Java 后端需求时，Hades 会按影响面触发这些规范：

| 触发条件 | 使用规范 |
| --- | --- |
| 准备写代码、改代码、review 代码 | `coding-discipline` |
| Java/Spring/MyBatis 代码实现 | `java-coding-standard` |
| Controller / Service / Mapper / 事务 / 分页 / 异步 / 外部调用 | `springboot-service-patterns` |
| 新增接口、权限、敏感数据、导入导出、SQL 注入风险、密钥配置 | `springboot-security-review` |
| MySQL MCP、写 SQL、DELETE、DDL、EXPLAIN、数据验证 | `mysql-db-guard` |
| Mapper、SQL、列表、搜索、统计、导出、分页、排序、索引 | `sql-performance-review` |
| 构建、编译、测试编译失败 | `java-build-fix` |
| 测试计划、回归测试、手动验证证据 | `java-test-strategy` |
| 仓库有 `.codegraph/`、需要调用链、影响面、符号定位或 CodeGraph 安装/初始化 | `codegraph-environment-guard` + `codegraph-context-guard` |
| 代码写完后对照 PRD/OpenSpec/tasks、检查漏做/多做/文档同步/证据缺口 | `post-implementation-check` |
| 代码审查 | `java-backend-review` |
| PR 前合并准备 | `pr-quality-gate` |
| 大重构、跨模块迁移、架构调整 | `large-refactor-workflow` |

## PRD 阶段

PRD 文件必须在目标项目根目录：

```text
docs/<中文功能名>/
```

固定创建：

```text
<功能名>文档入口.md
<功能名>需求PRD.md
```

按影响范围创建：

```text
<功能名>原型行为规则.md
<功能名>前端接口设计.md
<功能名>数据库设计.md
<功能名>技术设计.md
```

没有涉及的范围不要创建空文档，改在 `<功能名>文档入口.md` 的 `未涉及范围` 中说明原因。

PRD 写完后必须停下来让用户确认：

```text
PRD 文档已生成，请确认是否可以进入 OpenSpec。
```

## OpenSpec 阶段

PRD 确认后，用户可以说：

```text
PRD 已确认，请生成 OpenSpec。
```

或：

```text
/opsx:propose add-order-export
```

`/opsx:propose` 支持两种输入：

```text
/opsx:propose <kebab-case-change-name>
/opsx:propose <natural-language-description>
```

如果用户说的是自然语言，Codex 需要转换成 kebab-case change name。

运行任何 OpenSpec 命令前，Codex 必须先使用 `openspec-environment-guard` 检查环境：

macOS/Linux：

```bash
command -v openspec
openspec --version
```

Windows PowerShell：

```powershell
where.exe openspec
openspec --version
```

如果未安装 OpenSpec，Codex 可以在用户明确同意后执行安装：

```bash
npm install -g @fission-ai/openspec@latest
```

Windows 使用：

```powershell
npm.cmd install -g @fission-ai/openspec@latest
```

如果 npm 不存在，Codex 不能自动安装 Node.js，只能提示用户先按团队标准安装 Node.js/npm。

执行规则：

```bash
openspec new change "<name>"
openspec status --change "<name>" --json
openspec instructions <artifact-id> --change "<name>" --json
openspec status --change "<name>"
```

必须按 OpenSpec status 返回的依赖顺序生成 artifacts，并在所有 `applyRequires` 完成后停下来让用户确认。

## 实现阶段

OpenSpec 确认后，用户可以说：

```text
执行计划。
```

或：

```text
/opsx:apply
```

或：

```text
/opsx:apply add-order-export
```

实现前必须：

1. 解析 change name。
2. 使用 `openspec-environment-guard` 确认 OpenSpec CLI 和项目初始化。
3. 如果仓库存在 `.codegraph/` 或需要调用链/影响面，使用 `codegraph-environment-guard` 确认 CodeGraph CLI/MCP/索引。
4. 运行 `openspec status --change "<name>" --json`。
5. 运行 `openspec instructions apply --change "<name>" --json`。
6. 读取 `contextFiles` 中列出的所有文件。
7. 应用 `coding-discipline`，明确假设、范围、不做什么、验证和风险。
8. 对 Java/Spring/MyBatis/MySQL 影响面应用对应规范。
9. 展示进度和剩余任务。
10. 按 task 循环实现。
11. 每完成一个 task，立即把 checkbox 从 `- [ ]` 改成 `- [x]`。
12. 代码写完后执行 `post-implementation-check`，全面对照 PRD / OpenSpec / tasks 与代码。
13. 自检通过后再运行最终验证，并进入 review gate。

如果任务不清楚、PRD/OpenSpec 不一致、contextFiles 缺失、测试失败改变范围，必须暂停并说明原因。

## 实现后自检阶段

代码写完后、最终验证前必须生成“实现后自检结果”，至少检查：

- PRD / OpenSpec / tasks 的每个验收点是否有代码、配置、测试或文档证据。
- 是否漏做、误做、多做，是否出现无关重构、顺手优化、新依赖、新接口、新字段或新状态流转。
- 实现中发现的新规则、接口字段、数据库约束、安全规则、回滚方案、验证要求是否需要同步 PRD / OpenSpec。
- Java 代码规范、Spring 分层、MyBatis SQL、权限、安全、日志是否命中专项规范。
- 是否需要补单测、集成测试、手动验证、SQL `EXPLAIN`、影响行数或回滚方案。

自检结果出现 `NEEDS_SYNC`、`NEEDS_FIX` 或 `NEEDS_EVIDENCE` 时，不能进入最终验证和 Review Gate；必须先同步文档、修复代码或补证据。

## Review 阶段

实现后自检和验证完成后必须告诉用户：

```text
改动已完成、实现后自检已通过并通过验证，现在可以进行 review。你可以让我执行代码审查、根据反馈修改，或准备归档计划。
```

用户可以继续说：

```text
执行代码审查。
```

Java 后端 Review 会按影响面使用 `java-backend-review`、`springboot-security-review`、`sql-performance-review`、`mysql-db-guard`、`java-test-strategy` 和 `pr-quality-gate`。

```text
根据 review 反馈修改。
```

```text
归档计划。
```

## 归档阶段

用户明确要求归档时才执行：

```text
归档计划。
```

或：

```text
/opsx:archive
```

归档前必须验证：

```bash
openspec validate <name>
openspec archive <name> --yes
```

如果项目维护 PRD 入口状态，也要同步更新 `<功能名>文档入口.md` 中的 OpenSpec 信息。

## 常用提示词

完整需求：

```text
请使用 Hades Openspec Java Workflow 处理这个需求：<需求内容>。先生成 PRD 文档，不要写代码。
```

PRD 改动：

```text
刚刚的规则改了：<新规则>。请同步更新 PRD 和 OpenSpec，然后再继续。
```

生成 OpenSpec：

```text
PRD 已确认，请创建 OpenSpec change，change 名称按 OpenSpec 自己规则生成。
```

执行实现：

```text
OpenSpec 已确认，请执行计划。
```

实现后自检：

```text
代码写完后，请先执行实现后自检，再进入最终验证和 review。
```

审查：

```text
请进入 review gate，先做代码审查，重点看 bug、回归风险和缺失测试。
```

归档：

```text
review 已确认，可以归档计划。
```
