# 安装配置指南

本文说明 Hades OpenSpec Workflow 依赖哪些工具，以及哪些平台需要单独配置账号或 connector。

## 插件本身需要什么

安装本插件只需要：

1. Codex 支持本地 plugin。
2. 本仓库 clone 到本地插件源码目录。
3. personal marketplace 中存在 `hades-openspec-workflow` 条目。
4. 执行 `codex plugin add hades-openspec-workflow@personal`。

插件安装不需要 Asana、GitHub、Stripe、Cloudflare、Notion、Figma、Canva 的 key。

## OpenSpec

目标项目需要安装 OpenSpec CLI：

```bash
npm install -g @fission-ai/openspec@latest
cd <your-project>
openspec init
openspec config profile
openspec update
```

常用命令：

```bash
openspec list
openspec status --change "<name>"
openspec status --change "<name>" --json
openspec instructions apply --change "<name>" --json
openspec validate <name>
openspec archive <name> --yes
```

## 外部平台 connector

外部平台是否需要 key，取决于你用的是 Codex 内置 connector、ChatGPT plugin、公司 MCP server，还是本地 CLI。

| 平台 | 常见认证方式 | Hades 使用方式 |
| --- | --- | --- |
| Asana | OAuth 或 personal access token | 读取任务内容、评论、字段、状态；必要时更新任务状态。 |
| GitHub | OAuth、Git credential、SSH key、GitHub App | 读取仓库、分支、commit、PR、review；不创建 Issue。 |
| Notion | OAuth 或 integration token | 读取 PRD、背景文档、知识库；可按用户确认写回记录。 |
| Figma | OAuth 或 Figma token | 读取设计稿、节点信息、交互线索。 |
| Stripe | OAuth 或 restricted API key | 读取支付、订阅、退款上下文；写操作必须强确认。 |
| Cloudflare | OAuth 或 API token | 检查部署、DNS、Workers、Pages；写操作必须强确认。 |
| Canva | OAuth | 读取或生成设计相关资料，按可用 connector 能力决定。 |
| 内部数据库 MCP | 公司 MCP 配置、只读或受控写账号 | 查询内部业务数据；写入必须按 MCP 安全规则确认。 |

建议原则：

- 不把 key 写入本仓库。
- 不把 key 写入 PRD、OpenSpec 或聊天输出。
- 优先使用 Codex 已安装 connector 或组织 MCP。
- 生产写操作必须先展示目标、影响范围和回滚方式。
- 数据库 DELETE、DDL、退款、DNS、部署切换等高风险动作必须获得用户明确授权。

## GitHub 使用边界

Hades OpenSpec Workflow 不创建 GitHub Issues。原因是本 workflow 以 PRD + OpenSpec 作为需求和变更事实来源。

GitHub 可用于：

- clone / pull / push 仓库
- 创建分支
- commit
- pull request
- review
- 读取 PR diff 和评论

GitHub 不用于：

- 自动创建 Issues
- 用 Issue 替代 PRD
- 用 Issue 替代 OpenSpec artifacts

## 内部数据库 MCP 建议

内部数据库 MCP 推荐分层：

| 权限 | 建议 |
| --- | --- |
| 只读查询 | 默认允许，适合需求分析、排查、验收辅助。 |
| INSERT / UPDATE | 先展示 SQL、影响范围、回滚方案，再等用户确认。 |
| DELETE | 先 SELECT 预览影响行，再强确认。 |
| DDL | 默认只允许非破坏性变更，并要求强确认。 |
| DROP / TRUNCATE / 权限变更 | 默认拒绝，除非组织另有明确审批流程。 |

## 项目级约定

建议目标项目根目录包含：

```text
AGENTS.md
openspec/
docs/
```

PRD 生成路径：

```text
docs/<中文功能名>/
```

OpenSpec change 路径：

```text
openspec/changes/<change-name>/
```

如果项目已经有自己的 `AGENTS.md`，以项目级约定为准，但不得跳过 Hades 的 PRD 确认、OpenSpec 确认、review gate 和同步规则。
