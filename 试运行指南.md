# 试运行指南

本文用一个低风险 Java/MySQL 需求验证插件是否能正确触发 PRD、OpenSpec、实现、实现后自检、Java/MySQL 规范和 review gate。

## 前置条件

1. 已安装 `hades-openspec-java-workflow@personal`。
2. 新开了 Codex 会话。
3. 当前目标项目已初始化 OpenSpec。

检查 OpenSpec：

```bash
openspec list
```

如果目标项目还没有 OpenSpec：

```bash
npm install -g @fission-ai/openspec@latest
cd <your-project>
openspec init
openspec config profile
openspec update
```

## 试运行需求

在 Codex 中输入：

```text
请使用 Hades Openspec Java Workflow 处理这个需求：给后台订单列表增加 CSV 导出入口，只导出当前筛选结果，不新增数据库字段。先生成 PRD，不要写代码。
```

期望行为：

1. Codex 进入需求分析。
2. 在 `docs/<中文功能名>/` 下生成 PRD 文档组。
3. 固定创建：
   - `<功能名>文档入口.md`
   - `<功能名>需求PRD.md`
4. 因为涉及 UI 和接口，应该创建：
   - `<功能名>原型行为规则.md`
   - `<功能名>前端接口设计.md`
5. 因为“不新增数据库字段”，不应该创建空的 `<功能名>数据库设计.md`，而是在入口文档中说明数据库未涉及。
6. PRD 生成后停下来请求确认，不进入 OpenSpec。

## PRD 确认后

输入：

```text
PRD 已确认，请生成 OpenSpec。
```

期望行为：

1. Codex 运行 `openspec new change "<name>"`。
2. 运行 `openspec status --change "<name>" --json`。
3. 根据 `openspec instructions <artifact-id> --change "<name>" --json` 生成 artifacts。
4. 完成 `applyRequires` 后停下来请求用户确认 OpenSpec。

## OpenSpec 确认后

输入：

```text
OpenSpec 已确认，请执行计划。
```

期望行为：

1. Codex 选择 change。
2. 运行 `openspec instructions apply --change "<name>" --json`。
3. 读取 `contextFiles`。
4. 逐个实现 task。
5. 每完成一个 task 立即勾选。
6. 代码写完后生成“实现后自检结果”。
7. 自检通过后运行验证。
8. 进入 review gate。

同时期望：

- 实现前应用 `coding-discipline`。
- Java 代码应用 `java-coding-standard`。
- Spring Boot 分层应用 `springboot-service-patterns`。
- 导出/查询 SQL 应用 `sql-performance-review`。
- MySQL 查询或 EXPLAIN 应用 `mysql-db-guard`。
- 实现后应用 `java-test-strategy`。
- 进入最终验证前应用 `post-implementation-check`，检查是否漏做、多做、需要同步 PRD/OpenSpec、缺测试、缺 EXPLAIN 或缺回滚方案。

## Review 后归档

输入：

```text
review 已确认，可以归档计划。
```

期望行为：

1. Codex 运行 `openspec validate <name>`。
2. 验证通过后运行 `openspec archive <name> --yes`。
3. 如项目维护 PRD 入口状态，同步更新入口文档。

## 失败判断

如果 Codex 直接写代码，说明 workflow 没有被正确触发。

如果 Codex 生成 OpenSpec 前没有请求 PRD 确认，说明 PRD gate 没有生效。

如果 Codex 完成实现后直接归档，说明 review gate 没有生效。

如果 Codex 完成代码后没有先输出实现后自检结果就进入最终验证或 review，说明 post-implementation-check 没有生效。

如果 Codex 创建 GitHub Issue，说明 GitHub 使用边界没有生效。

## 数据库 MCP 试运行

如果团队已经配置内部数据库 MCP，可以用一个只读问题验证 `mysql-db-guard`：

```text
请使用 Hades Openspec Java Workflow，通过内部数据库 MCP 只读查询最近 1 条订单记录的字段结构，用于理解订单列表导出 PRD。不要输出敏感明细，不要执行写操作。
```

期望行为：

1. Codex 识别数据库访问需求。
2. 只执行 `SELECT`、`SHOW`、`DESCRIBE` 或 `EXPLAIN` 类型操作。
3. 查询有字段限制或行数限制。
4. 输出总结，而不是大批量明细。
5. 如果查询结果影响 PRD 或 OpenSpec，先同步文档。
