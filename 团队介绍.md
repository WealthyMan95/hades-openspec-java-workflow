# 团队介绍

Hades OpenSpec Workflow 适合希望让 Codex 严格遵守“需求文档 -> OpenSpec -> 实现 -> 验证 -> review -> 归档”流程的团队。

## 适合场景

- 多个需求来源并存：Asana、Notion、用户对话、Figma、Stripe、Cloudflare、GitHub PR、内部系统。
- 团队希望 PRD 和 OpenSpec 成为可追踪事实来源。
- 不希望 AI 在需求不清楚时直接改代码。
- 不希望用 GitHub Issues 替代产品和技术变更文档。
- 希望把 review 作为实现后的固定关卡。

## 不适合场景

- 只想要一个能直接操作所有平台的万能 connector。
- 不打算使用 OpenSpec。
- 不需要 PRD 或变更确认，偏好快速口头改动。
- 希望自动创建 GitHub Issues 来追踪需求。

## 推荐团队约定

1. 每个功能需求必须先有 `docs/<中文功能名>/`。
2. 每次实现前必须有 active OpenSpec change。
3. PRD 和 OpenSpec 不一致时，先同步文档，再写代码。
4. 完成实现后先 review，不自动 archive。
5. 归档必须由用户明确触发。
6. 外部平台 key 和数据库账号由团队独立管理，不进入插件仓库。
