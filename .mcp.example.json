{
  "mcpServers": {
    "internal-mysql-readonly": {
      "command": "npx",
      "args": [
        "-y",
        "<replace-with-your-mysql-mcp-server-package>"
      ],
      "env": {
        "MYSQL_CONNECTION_STRING": "${HADES_MYSQL_READONLY_URL}"
      }
    }
  }
}
